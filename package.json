{
  "name": "scholarmatch-api",
  "version": "1.0.0",
  "description": "ScholarMatch API Functions",
  "main": "api/signup.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "node-fetch": "^3.3.2"
  }
}
